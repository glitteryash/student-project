<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=data.name%>'s Page</title>
  </head>
  <body>
    <table>
      <tr>
        <td>ID</td>
        <td><%= data.id %></td>
      </tr>
      <tr>
        <td>Name</td>
        <td><%= data.name %></td>
      </tr>
      <tr>
        <td>Age</td>
        <td><%= data.age %></td>
      </tr>
      <tr>
        <td rowspan="2">Scholarship</td>
        <td>Merit <%= data.scholarship.merit %></td>
      </tr>
      <tr>
        <td>Other <%= data.scholarship.other %></td>
      </tr>
    </table>
    <button onclick="window.location.href='/students/edit/<%= data.id %>'">
      fix data
    </button>
    <button onclick="window.location.href='/students'">back to list</button>
    <form action="/students/<%= data.id %>?_method=DELETE" method="POST">
      <button
        onclick="return confirm('Are you sure you want to delete the student?')"
      >
        delete
      </button>
    </form>
  </body>
</html>
